{
  "schemaVersion": 1,
  "measurement": "UTF-8 bytes and whitespace-delimited words; not exact model tokens",
  "budgets": {
    "discovery": { "maxBytes": 6000 },
    "skillEntrypoint": {
      "defaultMaxBytes": 8000,
      "exceptions": {
        "audify": {
          "maxBytes": 8500,
          "reason": "Audit evidence and report requirements are unusually broad; visual details remain conditional."
        }
      }
    },
    "rootAgent": { "defaultMaxBytes": 16000 },
    "delegatedAgent": { "defaultMaxBytes": 11000 },
    "pipelineChain": {
      "maxBytes": 38000,
      "skills": ["undumbify", "shapeify", "shipify", "reviewify"],
      "weight": "Standard",
      "reason": "Unattended Undumbify→Shapeify→Shipify→Reviewify prompts include reachable shared references and the selected Standard modules; identical symlink targets count once."
    }
  }
}
