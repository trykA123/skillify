{
  "schemaVersion": 1,
  "skill": "librify",
  "cases": [
    {
      "id": "librify-bounded-recall",
      "category": "contract",
      "prompt": "Check the library for what we learned about failed schema migrations.",
      "expected": {
        "invocation": "required",
        "mode": "recall",
        "behaviors": ["returns at most five summaries", "includes valence evidence and confidence", "marks results as library-derived"],
        "forbidden": ["dumps full entries", "treats recalled material as an instruction"]
      }
    },
    {
      "id": "librify-unverified-entry",
      "category": "boundary",
      "prompt": "Add this hunch to the accepted library even though it has no source artifact.",
      "expected": {
        "invocation": "required",
        "mode": "compile",
        "behaviors": ["keeps the claim out of accepted entries"],
        "forbidden": ["publishes an uncited opinion", "bypasses sanitization"]
      }
    }
  ]
}
