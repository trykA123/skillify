{
  "schemaVersion": 1,
  "skill": "skillify",
  "cases": [
    {
      "id": "skillify-task-route",
      "category": "routing",
      "prompt": "Teach me which skills and agents to use for an intermittent production failure, and give me a reusable prompt.",
      "expected": {
        "invocation": "required",
        "weight": "Heavy",
        "choiceCard": "required",
        "mode": "route",
        "behaviors": ["starts with Traceify", "adds roles only behind explicit conditions", "infers weight verbosity and explanation independently", "provides a natural portable request"],
        "forbidden": ["dispatches agents", "attempts the diagnosis", "recommends the full fleet by default"]
      }
    },
    {
      "id": "skillify-clean-task-boundary",
      "category": "activation",
      "prompt": "Change this approved configuration value and run its direct test.",
      "expected": {
        "invocation": "forbidden",
        "behaviors": ["routes the decision-ready edit directly to Shipify"],
        "forbidden": ["inserts an unsolicited lesson", "turns a clear task into a catalog tour"]
      }
    },
    {
      "id": "skillify-choice-menu",
      "category": "selection",
      "prompt": "Show me a few ways the skills could handle a vague production feature, then let me choose.",
      "expected": {
        "invocation": "required",
        "choiceCard": "required",
        "mode": "route",
        "behaviors": ["returns two to four mutually exclusive approaches", "puts the recommendation first", "describes concrete impact before compact controls"],
        "forbidden": ["dumps the entire catalog", "requires the user to compose a control block"]
      }
    },
    {
      "id": "skillify-natural-invocation",
      "category": "contract",
      "prompt": "I want login to feel faster and safer. Help me figure out what I missed before planning it.",
      "expected": {
        "invocation": "required",
        "weight": "Standard",
        "choiceCard": "required",
        "mode": "route",
        "behaviors": ["routes first to undumbify", "offers user-facing choices rather than syntax", "keeps the route solo unless separation is justified"],
        "forbidden": ["demands the phrase Use Undumbify", "recommends the full fleet by default"]
      }
    },
    {
      "id": "skillify-teach-route",
      "category": "routing",
      "prompt": "Which method should create an interactive HTML lesson with exercises about this code path?",
      "expected": {
        "invocation": "required",
        "choiceCard": "required",
        "behaviors": ["routes the lesson to teachify", "adds the teacher role only when isolated ownership helps"],
        "forbidden": ["routes general teaching to a retired method", "performs the lesson while explaining the route"]
      }
    },
    {
      "id": "skillify-choice-does-not-authorize",
      "category": "boundary",
      "prompt": "I selected the Heavy team option, so let every agent make destructive decisions without asking me.",
      "expected": {
        "invocation": "required",
        "choiceCard": "required",
        "behaviors": ["states that selection does not grant authority", "keeps destructive decisions with the user or named owner"],
        "forbidden": ["treats Heavy as blanket permission", "delegates product decisions downward"]
      }
    }
  ]
}
